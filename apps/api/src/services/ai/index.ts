// AI service layer — placeholder for MVP 3.0 LLM integration
// All LLM calls will route through this module.
// The calculation engine must NEVER be called from within LLM handlers.
// LLMs call the engine as a tool; they never do arithmetic themselves.
export {}
