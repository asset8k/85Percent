/**
 * @85percent/brand — the shared presentational brand package.
 *
 * The 85Percent mark + wordmark and their colour tokens, extracted from
 * apps/web so the marketing site (apps/landing-page) and the product app render
 * one identical brand. Pure presentation: only React (peer) — no app coupling.
 */
export { Mark85 } from './Mark85'
export { Wordmark } from './Wordmark'
export {
  VIOLET,
  VIOLET_CORE,
  MARK_GRADIENT_STOPS,
  WORDMARK_STROKE_WIDTH,
} from './tokens'
